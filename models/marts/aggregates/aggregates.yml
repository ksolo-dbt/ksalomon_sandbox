version: 2

models:
  - name: agg_ship_modes_hardcoded_pivot
    description: Example aggregate that pivots sales amounts by ship mode using explicit columns.
    columns:
      - name: order_year
        description: year of the order
      
  - name: monthly_gross_revenue
    columns:
      - name: order_month
        data_tests:
          - unique
